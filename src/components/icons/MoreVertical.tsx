import { FiMoreVertical } from "react-icons/fi";
import { IconBaseProps } from "react-icons";

interface Props extends IconBaseProps {}

const MoreVertical = (props: Props) => <FiMoreVertical {...props} />;

export default MoreVertical;
