import { IconBaseProps } from "react-icons";
import { MdWork } from "react-icons/md";
import React from "react";

interface Props extends IconBaseProps {}

const Work = (props: Props) => <MdWork {...props} />;

export default Work;
