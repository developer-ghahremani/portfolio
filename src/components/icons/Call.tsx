import { IconBaseProps } from "react-icons";
import { IoCall } from "react-icons/io5";
import React from "react";

interface Props extends IconBaseProps {}

const Call = (props: Props) => <IoCall {...props} />;

export default Call;
