import { FaTelegramPlane } from "react-icons/fa";
import { IconBaseProps } from "react-icons";

interface Props extends IconBaseProps {}

const Telegram = (props: Props) => <FaTelegramPlane {...props} />;

export default Telegram;
