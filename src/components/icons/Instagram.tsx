import { FiInstagram } from "react-icons/fi";
import { IconBaseProps } from "react-icons";

interface Props extends IconBaseProps {}

const Facebook = (props: Props) => <FiInstagram {...props} />;

export default Facebook;
