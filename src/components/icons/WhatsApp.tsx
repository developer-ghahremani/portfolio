import { FaWhatsapp } from "react-icons/fa";
import { IconBaseProps } from "react-icons";

interface Props extends IconBaseProps {}

const WhatsApp = (props: Props) => <FaWhatsapp {...props} />;

export default WhatsApp;
